import java.io.*;import java.lang.reflect.*;import java.util.*;public class App{    public static void main(String[] args)    {        for (String s : args)        {            switch (s)            {                case "Howdy": System.out.println("It works!"); break;                default: System.out.println("Meh"); break;            }        }                int i = 1_000_000;        System.out.println(i);        try        {            Field f = App.class.getField("nonExistent");            f.setInt(null, 12);        }        catch (IllegalAccessException | NoSuchFieldException ex)        {            ex.printStackTrace();        }        List<Map<String, List<String>>> anagrams = new ArrayList<>();                try (FileReader fr = new FileReader("App.java");             BufferedReader br = new BufferedReader(fr))        {            String line = null;            while ((line = br.readLine()) != null)                System.out.println("> " + line);        }        catch (IOException ioEx)        {            ioEx.printStackTrace();        }    }}